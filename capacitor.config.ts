import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minoscao.catlock',
  appName: '猫咪专注',
  webDir: 'dist',
  android: {
    backgroundColor: '#f6ead6'
  }
};

export default config;
